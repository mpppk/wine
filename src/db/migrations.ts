// このコードが前提とする DB スキーマの世代。ヘルスチェック(/api/health)が、実際に D1 へ
// 適用済みのマイグレーションと突き合わせて「新スキーマ×旧コード」のズレを検出する(#336)。
//
// デプロイは「ビルド成功 → db:migrate:remote → 新 Worker 反映」の順で走るため、適用と反映の
// 間には必ず短い窓がある(docs/deployment.md)。そこでデプロイが失敗すると、ズレたまま固定化
// する。CI は緑・ホームも OAuth メタデータも生きているのにアプリだけ全滅、という状態を
// スモークが素通りしないよう、コード側が期待する世代を値として持つ。
//
// **drizzle/ に連番SQLを足したらこの定数も更新する**(`migrations.test.ts` が強制する)。

/** `drizzle/` の連番マイグレーションのうち、このコードが前提とする最新のファイル名。 */
export const EXPECTED_LATEST_MIGRATION = "0035_user_locale";

-- Issue #574: 目撃記録に「そのワインが写っていた写真」の一覧を持たせる。
--
-- 一括登録のレビュー画面は、解析の画像-ワイン対応(`photoIndexes`)を持ち回っている
-- にもかかわらず、登録時には先頭の1枚(`photoIndex`)だけを目撃記録に載せていた。
-- 検出されたワインそれぞれに少なくとも分析に利用した画像を登録するため、対応する
-- 写真番号の一覧を JSON 配列で永続化する列を足す。
--
-- NULL 許容で足すだけなので、この列を書かない旧コードの INSERT もそのまま通る
-- (expand-and-contract の対象ではない)。既存行は NULL = 「先頭1枚だけの従来行」で、
-- 読み取りは `photo_index` へ退避する(後方互換)。`photo_index` への書き込みも続ける
-- ので、「新スキーマ×旧コード」の窓でも表示が欠けない。
--
-- パース失敗・範囲外の番号は読み取り側で落とす(存在しないキーの URL を作ると
-- リンク切れの画像が並ぶ)。書き込み側の形の検証は zod
-- (`wineSightingFields.photoIndexes`)が単一の関門として行う。
ALTER TABLE `wine_sighting` ADD COLUMN `photo_indexes` text;
